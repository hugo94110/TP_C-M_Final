{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/home.css') }}">
{% endblock %}

{% block title %}Vêtements Avant-Gardistes{% endblock %}

{% block body %}

<!-- <div class="videoContainer">
    <video autoplay muted loop class="homeVideo">
        <source src="https://player.vimeo.com/progressive_redirect/playback/1143724967/rendition/720p/file.mp4?loc=external&signature=a8233261d8d23c56852517bd9ec580331111462bd372d524605c5c305fb34f84" type="video/mp4">
        Your browser does not support the video tag.
    </video>
</div> -->

<div class="articlesContainer">
    {% if articles is defined and articles is not empty %}
        {% for article in articles %}
            <a class="article" href="{{ path('app_article_show', {'id': article.id}) }}">
                <div class="articleImg">
                    <img src="{{ article.imageLink }}" alt="{{ article.description }}">
                </div>
                <div class="articleDesc">
                    <p>{{ article.description }}</p>
                    <p>{{ article.price }}€</p>
                </div>
            </a>
        {% endfor %}
    {% else %}
        <p>Aucun produit disponible pour le moment.</p>
    {% endif %}
</div>

{% endblock %}