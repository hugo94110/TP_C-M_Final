<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>

        <header>
            <!-- <h1 class="headerLogo">Logo</h1> -->
            <!-- <img class="headerLogo" src="{{ asset('img/logo.png') }}" alt="Logo"> -->
            <div class="navHeader">
                <nav>
                    <a href="{{ path('app_home') }}">Accueil</a>
                    <a href="{{ path('app_home') }}">Qui sommes-nous ?</a>
                    <a href="{{ path('app_home') }}">Politique de confidentialité</a>
                    <a href="{{ path('app_home') }}">CGV</a>
                    <a href="{{ path('app_home') }}">CGU</a>
                </nav>
            </div>
            <div class="headerUser">
                {% if app.user %}
                    <a href="{{ path('app_profile') }}">Mon compte</a>
                {% else %}
                    <a href="{{ path('app_login') }}">Se connecter</a>
                {% endif %}
            </div>

        </header>
        <main>
            {% block body %}{% endblock %}
        </main>
        
        <footer>
        
        </footer>
        
    </body>
</html>
